mixin selecttree(records,level = 1)
    - const prefix = Array(level + 1).join("-- "); 
    each item in records
        option(value=item.id) #{prefix}#{item.title}
            if item.chilrend && item.chilrend.length > 0
                +selecttree(item.chilrend,level + 1)